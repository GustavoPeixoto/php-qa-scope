# Project name used by Docker Compose for images and resources.
export PROJECT_NAME="php-qa-scope"

# Absolute host path bind-mounted into the dev containers.
export HOST_WORKSPACE_DIR="/bind/mount/full/path"

# Workspace path mounted inside the containers.
export WORKSPACE_DIR="/${PROJECT_NAME}"
